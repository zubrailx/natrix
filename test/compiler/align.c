#include <criterion/criterion.h>

// static size_t cg_with_align(size_t size, size_t align) {
//   return ((size + offset - 1) / align + 1) * align - offset;
// }

Test(align, test) {
  // cr_expect_eq(8, cg_with_align(5, 4, 8));
  // cr_expect_eq(8, cg_with_align(5, 3, 8));
  // cr_expect_eq(8, cg_with_align(5, 2, 8));
  // cr_expect_eq(8, cg_with_align(5, 0, 8));
  // cr_expect_eq(8, cg_with_align(3, 3, 8));
}
